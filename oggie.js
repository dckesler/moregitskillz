let job = elm => {
  Array.isArray(elm)
    ? console.log("its an array")
    : console.log("it something else");
};

const myArr = [1, 2, 3, 4];

let time = val => {
  console.log(val);
};

let myObj = {
  me: "spencer",
  age: "20?"
};
let arrow = "arrow";
function jump(val, val2) {
  let around;
  if (val === va2) {
    around = val;
  } else {
    around = val2;
  }
  return around;
}

jump(1, 2);
