let job = elm => {
  Array.isArray(elm)
    ? console.log("its an array")
    : console.log("it something else");
};

let myObj = {
  me: "spencer",
  age: "20?"
};
let arrow = "arrow";
function jump(val, val2) {
  let around;
  if (val === va2) {
    around = val;
  } else {
    around = val2;
  }
  return around;
}

jump(1, 2);
